benchmark:
  expected_exit: 0
  checks:
    - script: tests/test_production_manifest.py

project:
  name: pass_production_manifest_regressions
  output_dir: projects/pass_production_manifest_regressions
  artifacts_dir: artifacts
