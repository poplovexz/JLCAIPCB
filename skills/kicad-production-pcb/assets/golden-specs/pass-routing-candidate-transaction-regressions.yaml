benchmark:
  expected_exit: 0
  checks:
    - script: tests/test_routing_candidate_transaction.py

project:
  name: pass_routing_candidate_transaction_regressions
  output_dir: projects/pass_routing_candidate_transaction_regressions
  artifacts_dir: artifacts
