benchmark:
  expected_exit: 1
  checks:
    - script: spec_freeze_check.py
      args: [--before-generation]

project:
  name: fail_spec_freeze_missing
  stage: local-mvp
  output_dir: projects/fail_spec_freeze_missing
  artifacts_dir: artifacts
